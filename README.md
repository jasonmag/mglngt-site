# mglngt-site

Official website for Maglangit Non-Specialized Wholesale Trading, built with
Next.js App Router and configured for static export deployment.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Build for Cloudflare Pages

```bash
npm run build
```

The static output is generated in `out/`.
